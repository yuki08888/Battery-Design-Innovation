id: ''
name: 機能割当（SysMLオントロジー.yaml）
slash_command: migrated-functionalallocation
description: 'オントロジー「SysMLオントロジー.yaml」の v2 → v3 移行により自動生成された手順書です。

  旧 ModelingRules の Procedure="FunctionalAllocation" に該当する 1 件の規約を手順として書き起こしています。


  注意:

  - 各ステップの内容は規約の文面をそのまま転記したものです。実際の作業手順として読めるように整理してください。

  - 同じ内容はオントロジー側の規約 (conventions.rules) にも設定されています。'
use_when: 機能割当に関する作業を行うとき。
steps:
- 'no': 1
  title: 振る舞いは実行主体へ allocate で割り当てる
  instruction: 主要な Activity は allocate 関係で実行主体の Block (Subsystem/Component) に割り当てる。割当が無い機能は実装責任が曖昧。
  tools: []
  done_when: ''
strict: false
allow_skip: true
max_steps_per_stage: 12
